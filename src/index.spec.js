// most test coverage is in the 'ebird-histogramr' repo.
describe('the histogramr-cli', () => {

    it('should have software tests', () => {

        // but('I haven\'t figured out any yet'); :P
    });
});